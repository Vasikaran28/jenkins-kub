pipeline {
    agent any

    environment {
        ACR_NAME = 'jenkinscontainer7'
        IMAGE_NAME = 'python-app'
        IMAGE_TAG = 'v1'
        ACR_LOGIN_SERVER = "${ACR_NAME}.azurecr.io"
    }

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Test Docker') {
            steps {
                script {
                    sh 'docker --version' // Test if Docker is available
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                script {
                    dockerImage = docker.build("${ACR_LOGIN_SERVER}/${IMAGE_NAME}:${IMAGE_TAG}")
                }
            }
        }
        stage('Push to ACR') {
            steps {
                script {
                    docker.withRegistry("https://${ACR_LOGIN_SERVER}", 'ACR_Credentials') {
                        dockerImage.push()
                    }
                }
            }
        }

        stage('Deploy to AKS') {
            steps {
                script {
                    sh """
                    kubectl apply -f deployment.yaml
                    """
                }
            }
        }
    }
}
